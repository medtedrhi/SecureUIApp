<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Activite de demonstration securite UX avec FLAG_SECURE et overlay. -->
        <activity
            android:name=".SecurityDemoActivity"
            android:exported="false" />

        <!-- Activite de demonstration des animations Material. -->
        <activity
            android:name=".AnimationDemoActivity"
            android:exported="false" />

        <!-- Activite de demonstration du mode clair et sombre. -->
        <activity
            android:name=".ThemeTestActivity"
            android:exported="false" />

        <!-- Activite principale lancee par l'icone de l'application. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Filtre qui rend MainActivity visible dans le lanceur Android. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
